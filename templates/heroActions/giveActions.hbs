<div class="actions">
    {{#each actions as |action|}}
    <div class="action{{#if action.drawn}} drawn{{/if}}">
        <input class="item-select" type="checkbox" name="action" value="{{action.value}}"
            data-dtype="JSON">
        <span class="item-name" data-action="expand">{{action.name}}</span>
        <span class="item-drawn">
            {{#if action.drawn}}
            <i class="fas fa-lock fa-fw" data-tooltip="{{@root.i18n 'drawn'}}"></i>
            {{else if @root.isUnique}}
            <i class="fas fa-unlock fa-fw"></i>
            {{/if}}
        </span>
        <div class="item-summary" hidden="hidden">
            <div class="item-description"></div>
        </div>
    </div>
    {{/each}}
</div>
<div class="buttons">
    <hr>
    <div>
        <input type="checkbox" name="message" checked>
        <span>{{i18n 'withMessage'}}</span>
    </div>
    {{#if isUnique}}
    <div>
        <input type="checkbox" name="drawn" checked>
        <span>{{i18n 'asDrawn'}}</span>
    </div>
    {{/if}}
</div>